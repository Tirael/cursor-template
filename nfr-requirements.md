---
version: 1.0
lastUpdated: 2026-02-04
status: Активен
---

# Нефункциональные требования EBookStore (v1.0)

> Полная версия NFR.
> Критический минимум зафиксирован в `.cursor/rules/nfr-core.mdc`
> и имеет приоритет при конфликте.

## 1. Доступность и восстановление

- **Доступность**: SLO ≥99.9% за год
- **Плановые работы**: ≤2 часа/неделя, суммарно ≤72 часа/год
- **RTO**: ≤2 часа
- **RPO**: ≤15 минут
- **Rollback релиза**: ≤15 минут

## 2. Производительность

- **UI (p95)**: ≤1 сек (каталог/карточка/библиотека)
- **API CRUD (p95)**: ≤500 мс
- **Поиск (p95)**: ≤800 мс
- **Оформление заказа (p95)**: ≤2 сек без учета внешнего PSP
- **Экспорт (до 50 страниц)**: ≤30 сек

## 3. Надёжность и устойчивость

- **Circuit breaker**: для внешних зависимостей (PSP, Email/SMS)
- **Retry**: экспоненциальный backoff (1s, 2s, 4s, 8s), максимум 4 попытки
- **Идемпотентность команд**: `correlationId` (UUIDv7) и/или `Idempotency-Key`
- **Гарантия событий**: Transactional Outbox для Integration Events
- **Graceful shutdown**: timeout ≤30 секунд

## 4. Безопасность и изоляция

- **Аутентификация**: OAuth2/OIDC (IdP)
- **Авторизация**: ABAC (`tenantId`, роль, контекст доступа)
- **Tenant isolation**: любые чтения/записи учитывают `tenantId`
- **Шифрование**: TLS 1.3 снаружи, минимум TLS 1.2 внутри; mTLS для gRPC
- **Секреты**: только Vault

## 5. Наблюдаемость

- **Логи**: JSON, обязательные поля `correlationId`, `tenantId`
- **Метрики**: Prometheus `/metrics`
- **Health/Readiness**: `/health`, `/ready`
- **Трейсы**: OpenTelemetry, корреляция по `correlationId`
- **Алертинг**: 5xx > 5/мин warning; > 20/мин critical

---

## История версий

### v1.0 (2026-02-04)

- Начальная версия NFR для шаблона EBookStore.
